#include <iostream>
#include <vector>

// === Variáveis globais ===
int n; // O número de sapos em cada memeplex
int m; // A quantidade de memeplexes

std::vector<std::vector<long double>> dist_matrix; // Matriz de distância das cidades

// === Constantes definidas ===
#define F (n*m) // Total de sapos no caso

// === Sinônimos e estruturas ===
using Frog = std::vector<int>; // Cada posição representa uma cidade visitada pelo sapo, em ordem
using Population = std::vector<Frog>; // Vetor de vetor de soluções


int main(){
}

